requests>=2.31
tzdata>=2024.1  # Windows has no built-in IANA tz database; zoneinfo needs this package there
psycopg[binary]>=3.1,<4
matplotlib>=3.8  # season report charts only (src/report_charts.py) -- no pandas, dataset is ~50 rows
